version: '3.8'

services:
  world-cup-app:
    build: .
    container_name: world-cup-app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/dev.db
    working_dir: /app
